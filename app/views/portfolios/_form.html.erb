<%= form_for(@portfolio) do |f| %>
  <% if @portfolio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@portfolio.errors.count, "error") %> prohibited this portfolio from being saved:</h2>

      <ul>
      <% @portfolio.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :portfolio_type %><br />
    <%= f.text_field :portfolio_type %>
  </div>
  <div class="field">
    <%= f.label :timeframe %><br />
    <%= f.text_field :timeframe %>
  </div>
 <div class="field">
    <%= f.label :location %><br />
    <%= f.text_field :location %>
  </div>
  <div class="field">
    <%= f.label :experience %><br />
    <%= f.text_area :experience %>
  </div>
  <div class="actions">
	<% if params[:action] == 'new'%>
    <%= submit_tag "Create Portfolio", :class=>"button" %>
	<%else%>
		<%= submit_tag "Update Portfolio", :class=>"button" %>
	<%end%>
<% end %>
