require "test_helper"

describe PostMailer do
  it "post_approval_request" do
    #post = Post.new
    #mail = PostMailer.post_approval_request(post)
    #mail.subject.must_equal "Post approval request"
    #mail.to.must_equal ["to@example.org"]
    #mail.from.must_equal ["from@example.com"]
    #mail.body.encoded.must_match "Hi"
  end
end
